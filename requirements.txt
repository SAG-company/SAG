streamlit
plotly
pandas
numpy
pillow
matplotlib

# ── 모델 추론용 (학습된 .keras EfficientNetB3 모델 로드에 필요) ──
# 프론트(inference.py)는 TensorFlow/Keras 모델을 불러와 추론합니다.
# CPU 전용 휠이라 Streamlit Cloud(GPU 없음)에서도 그대로 설치됩니다.
#
# 참고(배포 시):
#   - 최종 모델 final_models/efficientnetb3_final_*.keras (≈141MB)는 git에 올리지 않아
#     클라우드에는 없습니다. 이 경우 git에 포함된 models/exp2_transfer_finetuned.keras(128px)로
#     자동 폴백하며, 그마저 없으면 더미 모드로 동작합니다(앱은 끊기지 않음).
#   - 클라우드 빌드 용량/시간이 부담되면 아래 한 줄을 주석 처리해 더미 모드로 운영할 수 있습니다.
#   - ⚠️ TensorFlow는 Python 3.14용 휠이 아직 없습니다. 아래 환경 마커로 3.14에선 설치를
#     건너뛰어 앱이 (더미 모드로라도) 부팅되게 합니다. 실제 추론을 쓰려면 Streamlit Cloud의
#     Python 버전을 3.12(또는 3.13)로 설정하세요 → 그때만 TF가 설치되어 폴백 모델로 추론합니다.
tensorflow-cpu; python_version < "3.14"
